#!/bin/sh
HOST_WORKDIR=/mnt/workdir/host
for DIR in $(ls "$HOST_WORKDIR"); do
  echo "syncing: $DIR"
  if ! rsync -avc --delete-after "$HOST_WORKDIR/$DIR/" "macmini.voidedtech.com:~/Active/$DIR/"; then
    echo "unable to sync: $DIR"
  fi
done
