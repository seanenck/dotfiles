#!/usr/bin/env bash
BIN=bin/
CLONE=$BIN/voidedtech.com
rm -rf $BIN/
mkdir -p $BIN
git clone https://github.com/enckse/voidedtech.com $CLONE

podman build -f Containerfile -t website
git -C $CLONE status
