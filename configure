#!/usr/bin/env bash
_setup() {
	local f d
	for f in $(git ls-files | grep '^\.'); do
		d=$(dirname "$f")
		if [[ "$d" != "." ]]; then
			mkdir -p "$HOME/$d"
		fi
		ln -sf "$PWD/$f" "$HOME/$f"
	done 
}

_setup