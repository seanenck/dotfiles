#!/usr/bin/env bash
CONTAINERS=$HOME/.env/dotfiles/containers/
if [ -z "$1" ]; then
    echo "container required"
    exit 1
fi
CONTAINER=${CONTAINERS}$1
if [ ! -d "$CONTAINER" ]; then
    echo "invalid container"
    exit 1
fi
(cd $CONTAINER && make ${@:2})
