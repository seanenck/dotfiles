#!/usr/bin/env bash
rsync -avc center:store/home/archival/mail/ $HOME/store/mail
notmuch new

if [ -z "$1" ]; then
    echo "update completed"
    exit 0
fi
mkdir -p $HOME/.mblaze
notmuch search --output=files $@ | mseq | mscan -I
