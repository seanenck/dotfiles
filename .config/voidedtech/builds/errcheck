TAG    := 1.6.3
TAR    := errcheck.tar.gz
SRCDIR := errcheck-$(TAG)
SITE   := "https://github.com/kisielk/errcheck"
URL    := "$(SITE)/archive/v$(TAG).tar.gz"
BINARY := bin/errcheck
BUILD  := go build $(GOFLAGS) -o $(BINARY)
SUMS   := "6b5e8fb4637d58b72dc3dfcfc0e75cbc3703477e50b4e8234a6772104316975f  $(TAR)"

install:
	install -Dm755 $(BINARY) $(DESTDIR)/dev/errcheck
