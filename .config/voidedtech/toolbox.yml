defaults: [ripgrep, bat, git-delta, git, netcat]
boxes:
  - name: rpms
    packages: [rpmlint, make, rpmdevtools, mock]
  - name: dev
    packages: [go, cargo, make, nodejs-bash-language-server, rust, shellcheck, rust-analyzer]
