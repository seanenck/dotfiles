defaults: [ripgrep, bat, git-delta, git, netcat]
boxes:
  - name: go
    packages: [go, make]
  - name: rust
    packages: [cargo, rust, gcc-c++]
  - name: shell
    packages: [shellcheck, nodejs-bash-language-server]
  - name: mblaze
    packages: [make, gcc]
  - name: isync
    packages: [make, gcc, cyrus-sasl, libdb-devel, openssl-devel, cyrus-sasl-devel]
  - name: filebrowser
    packages: [go, npm]
  - name: rpms
    packages: [rpmlint, make, rpmdevtools, mock]

