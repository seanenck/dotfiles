NOBEEP=/tmp/nobeep.conf
depends=$(cat packages | grep -v -E "^(glib2|intel-ucode|mumble|wget)$")
grps=('i3' 'xorg' 'base-devel')
sudo pacman -S $depends $grps
echo "blacklist pcspkr" > $NOBEEP
sudo install -Dm644 $NOBEEP /etc/modprobe.d/nobeep.conf
