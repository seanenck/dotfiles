applications:
  rectangle:
    source:
      github:
        project: "rxhanson/Rectangle"
        release:
          asset: '.dmg$'
    extract:
      command: ["cp", "{{ $.Input }}", "{{ $.Output }}"]
