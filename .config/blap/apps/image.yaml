disable: true
applications:
  image:
    source:
      github:
        project: "seanenck/workstation-builds"
        branch:
          name: "master"
    commands:
      steps:
        - command: ["just", "hash={{ $.Vars.Tag }}", "needs"]
