applications:
  just:
    priority: 100
    github:
      project: "casey/just"
      release:
        asset: '.{{ if eq $.Arch "amd64" }}x86_64{{ else }}aarch64{{end}}(.*?){{ $.OS }}(.*?).tar.gz$'
    extract:
      nodepth: true
    commands:
      steps:
        - command: ["ln", "-sf", "{{ $.Vars.Directories.Root }}/{{ $.Name }}", "$PATHS/"]
        - command: ["cp", "-r", "{{ $.Vars.Directories.Root }}/completions/just.$COMPSH", "$COMPLETIONS/{{ $.Name }}"]
