applications:
  revive:
    github:
      project: "mgechev/revive"
      release:
        asset: "tarball"
    commands:
      steps:
        - command: ["go", "build", "-buildmode=pie", "-trimpath", "-modcacherw"]
        - command: ["ln", "-sf", "{{ $.Vars.Directories.Root }}/{{ $.Name }}", "$PATHS/"]
