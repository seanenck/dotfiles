#!/bin/bash
source $HOME/.bin/common
$HOME/.bin/dmenu-wrapper \
    --browser "/usr/bin/firefox" \
    --list "vlc pavucontrol passwords" \
    --temp "$USER_TMP" \
    --urls "${SYNCED_CONF}urls.csv" \
    "$@"
