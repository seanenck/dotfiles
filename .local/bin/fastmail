#!/usr/bin/perl
use strict;
use warnings;
use String::ShellQuote;

my @localcmd = (
    "kitty", "--title=fastmail", "/usr/bin/mutt", "-F",
    "/home/enck/.mutt/fastmail.muttrc"
);
system( "cd /home/enck/downloads && " . shell_quote @localcmd );
